import styles from './TokenWidget.module.scss';

export interface ITokenWidgetProps {

}

export const TokenWidget = (props: ITokenWidgetProps) => {
	return (
		<div className={styles.TokenWidget}>

		</div>
	);
};